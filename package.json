{
  "name": "humps",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ lucianhymer/humps",
    "create-local": "graph create --node http://localhost:8020/ lucianhymer/humps",
    "remove-local": "graph remove --node http://localhost:8020/ lucianhymer/humps",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 lucianhymer/humps"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.30.3",
    "@graphprotocol/graph-ts": "0.27.0"
  }
}
